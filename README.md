# Hoffman Soft Water

Industrial & commercial pretreatment — a division of Hoffman Water. Greater Cincinnati, OH.

Single-file static site (`index.html`). Hosted on Vercel.

## Local preview

Open `index.html` directly in a browser, or serve it:

```bash
npx serve .
```

## Deploy

Pushes to `main` redeploy automatically via Vercel.
